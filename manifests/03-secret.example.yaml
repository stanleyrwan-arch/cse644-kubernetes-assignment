# Template only: never commit a real secret value.
# Create the actual Secret at deployment time using the README command.
apiVersion: v1
kind: Secret
metadata:
  name: app-runtime-secret
  namespace: cse644
type: Opaque
stringData:
  DEMO_SECRET: "<set-at-deploy-time; do-not-commit>"
